const express = require('express');\nconst { getRecommendation, askQuestion, getInventoryInsights } = require('../controllers/aiController');\nconst { authMiddleware, adminMiddleware } = require('../middleware/auth');\n\nconst router = express.Router();\n\nrouter.post('/recommendation', authMiddleware, getRecommendation);\nrouter.post('/ask', authMiddleware, askQuestion);\n\nrouter.post('/inventory-insights', authMiddleware, adminMiddleware, getInventoryInsights);\n\nmodule.exports = router;
